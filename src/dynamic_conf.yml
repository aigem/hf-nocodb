http:
  routers:
    nocodb:
      rule: "PathPrefix(`/`) && !PathPrefix(`/serve`)"
      service: nocodb
    serve:
      rule: "PathPrefix(`/serve`)"
      service: serve
      middlewares:
        - stripprefix
  services:
    nocodb:
      loadBalancer:
        servers:
          - url: "http://localhost:7861"
    serve:
      loadBalancer:
        servers:
          - url: "http://localhost:7862"
  middlewares:
    stripprefix:
      stripPrefix:
        prefixes:
          - "/serve"
