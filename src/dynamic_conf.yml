http:
  routers:
    nocodb-router:
      rule: "PathPrefix(`/`)"
      service: nocodb-service
      entryPoints:
        - web
      priority: 1

  services:
    nocodb-service:
      loadBalancer:
        servers:
          - url: "http://localhost:7861"

